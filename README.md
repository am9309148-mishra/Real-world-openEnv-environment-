# Real-world-openEnv-environment-
OpenEnv is a custom-built, real-world inspired reinforcement learning (RL) environment designed using a standardized API (reset(), step(), get_state()). It simulates a delivery optimization problem, where an intelligent agent learns to navigate a grid, pick up items, and deliver them efficiently while maximizing rewards.
